import styled from '@emotion/styled';

export const P = styled.p`
  color: #fff;
  margin: 0;
  margin: 0;
  font-size: 18px;
`;

export const Name = styled.span`
  color: #fff;
  margin: 0;
  font-size: 18px;
  margin: 0;
`;

export const Button = styled.button`
  width: 25px;
  height: 25px;
  text-aling: center;
  padding: 0;
  font-size: 18px;
  border-radius: 5px;
  border: none;
  cursor: pointer;
  background-color: rgba(255, 255, 255, 0.816);
  transition: 250ms cubic-bezier(0.4, 0, 0.2, 1);
  :hover {
    background-color: rgba(227, 227, 227, 0.826);
  }
`;
